# Android SDK path (required for Appium UiAutomator2)
ANDROID_HOME=/Users/<your-username>/Library/Android/sdk

# Device / Emulator name (from `adb devices` or AVD Manager)
DEVICE_NAME=Pixel_8

# Path to APK (relative to project root)
APK_PATH=../app-release.apk
